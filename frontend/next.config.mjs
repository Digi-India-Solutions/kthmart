/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "res.cloudinary.com",
      "localhost",
      "picsum.photos",
      "api.kthmart.com",
      "https://",
      "http://",
    ],
  },
};

export default nextConfig;
