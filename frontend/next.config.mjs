/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "res.cloudinary.com",
      "api.goyattrading.shop",
      "picsum.photos",
      "api.goyattrading.shop",
      "https://",
      "http://",
    ],
  },
};

export default nextConfig;
